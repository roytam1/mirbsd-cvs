# $MirOS$

SRCDIRS distrib/ramtools usr.bin/ssh
SRCDIRS bin sbin usr.bin usr.sbin
SRCDIRS sys/arch/i386/stand libexec

LIBS	-L${BSDOBJDIR}/usr.bin/ssh/lib -lssh  -lssl -lcrypto -lutil -lotermcap -ll -lm -ldes -lkvm -lz
