# $MirOS: src/distrib/i386/common/listpre.full,v 1.2 2006/05/26 17:35:02 tg Exp $

SRCDIRS distrib/ramtools usr.bin/ssh usr.sbin/isdn
SRCDIRS bin sbin usr.bin usr.sbin
SRCDIRS sys/arch/i386/stand libexec

LIBS	-L${BSDOBJDIR}/usr.bin/ssh/lib -lssh  -lssl -lcrypto -lutil -locurses -ll -lm -ldes -lkvm -lz -ledit
