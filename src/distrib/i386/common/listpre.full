# $MirOS: src/distrib/i386/common/listpre.full,v 1.3 2006/05/26 19:03:28 tg Exp $

SRCDIRS distrib/ramtools usr.bin/ssh usr.sbin/isdn
SRCDIRS bin sbin usr.bin usr.sbin
SRCDIRS sys/arch/i386/stand libexec

LIBS	-L${BSDOBJDIR}/usr.bin/ssh/lib -lssh  -lssl -lcrypto -lutil -locurses -lm -ldes -lkvm -lz -ledit
