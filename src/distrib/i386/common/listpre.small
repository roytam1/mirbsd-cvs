# $MirOS$

SRCDIRS distrib/ramtools distrib/special
SRCDIRS bin sbin usr.bin usr.sbin
SRCDIRS sys/arch/i386/stand libexec

#LIBS	-lutil -lotermcap -ll -lm -ldes -lkvm -lz
#XXX this probably won't be enough
LIBS	-lutil -lotermcap -ll -lm
