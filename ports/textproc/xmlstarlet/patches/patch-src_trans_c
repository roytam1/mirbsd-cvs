$MirOS$
--- src/trans.c.orig	Mon Nov 22 02:27:59 2004
+++ src/trans.c	Wed May 28 23:20:00 2014
@@ -169,7 +169,7 @@ xsltExternalEntityLoader(const char *URL
         newURL = xmlMalloc(len);
         if (newURL != NULL)
         {
-            sprintf((char *) newURL, "%s/%s", paths[i], URL);
+            snprintf((char *) newURL, len, "%s/%s", paths[i], URL);
             ret = defaultEntityLoader((const char *)newURL, ID, ctxt);
             xmlFree(newURL);
             if (ret != NULL)
