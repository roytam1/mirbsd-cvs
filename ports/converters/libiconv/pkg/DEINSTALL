#!/bin/mksh
# $MirOS$

# exit on errors, use a sane path and install prefix
#
set -e
PATH=/bin:/usr/bin:/sbin:/usr/sbin
PREFIX=${PKG_PREFIX:-/usr/local}

cd ${PREFIX}/lib
. ../giconv/lib/libiconv.la
[[ -z $$dlname ]] || rm -f $(echo $dlname | sed s/libiconv/libgiconv/)

exit 0
