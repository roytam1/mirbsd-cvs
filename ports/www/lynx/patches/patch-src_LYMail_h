$MirOS$

	Fix spelling; suggested for upstream inclusion.

--- src/LYMail.h.orig	Mon Jun 13 00:18:30 2011
+++ src/LYMail.h	Sat Mar  3 18:20:22 2012
@@ -48,7 +48,7 @@ extern "C" {
 #endif
 
 /*
- * Ifdef's in case we have a working popen/pclose, useful for piping to the
+ * Ifdefs in case we have a working popen/pclose, useful for piping to the
  * mail program.
  */
 #ifndef CAN_PIPE_TO_MAILER
