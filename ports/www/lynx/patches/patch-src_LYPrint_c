$MirOS$

	Fix spelling; suggested for upstream inclusion.

--- src/LYPrint.c.orig	Mon Feb 13 02:45:53 2012
+++ src/LYPrint.c	Sat Mar  3 18:20:22 2012
@@ -1408,7 +1408,7 @@ char *GetFileName(void)
 #endif /* VMS */
 
     /*
-     * Expand tilde's, make filename absolute, etc.
+     * Expand tildes, make filename absolute, etc.
      */
     BStrCopy0(tbuf, "");
     if (!LYValidateFilename(&tbuf, &fbuf))
