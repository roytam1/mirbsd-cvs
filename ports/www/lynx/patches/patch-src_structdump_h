$MirOS$

	Fix spelling; suggested for upstream inclusion.

--- src/structdump.h.orig	Mon Jun 13 00:18:30 2011
+++ src/structdump.h	Sat Mar  3 18:20:22 2012
@@ -1,6 +1,6 @@
 /*
  *
- * Some macros to dump out formatted struct's via the trace file.  -KED
+ * Some macros to dump out formatted structs via the trace file.  -KED
  *
  */
 #ifndef STRUCTDUMP_H
