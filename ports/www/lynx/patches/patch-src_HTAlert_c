$MirOS$

	Fix spelling; suggested for upstream inclusion.

--- src/HTAlert.c.orig	Mon Feb 13 02:45:53 2012
+++ src/HTAlert.c	Sat Mar  3 18:20:22 2012
@@ -1128,7 +1128,7 @@ int HTConfirmPostRedirect(const char *Re
 #define okToSleep() (!crawl && !traversal && LYCursesON && !no_pause)
 
 /*
- * Sleep for the given message class's time.
+ * Sleep for the given message class' time.
  */
 void LYSleepAlert(void)
 {
