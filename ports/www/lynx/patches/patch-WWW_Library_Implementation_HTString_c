$MirOS$

	Fix spelling; suggested for upstream inclusion.

--- WWW/Library/Implementation/HTString.c.orig	Mon Feb 13 02:45:53 2012
+++ WWW/Library/Implementation/HTString.c	Sat Mar  3 18:20:21 2012
@@ -1307,7 +1307,7 @@ void HTSABCat0(bstring **dest, const cha
 }
 
 /*
- * Compare two bstring's for equality
+ * Compare two bstrings for equality
  */
 BOOL HTSABEql(bstring *a, bstring *b)
 {
@@ -1335,7 +1335,7 @@ void HTSABFree(bstring **ptr)
 }
 
 /*
- * Use this function to perform formatted sprintf's onto the end of a bstring.
+ * Use this function to perform formatted sprintf onto the end of a bstring.
  * The bstring may contain embedded nulls; the formatted portions must not.
  */
 bstring *HTBprintf(bstring **pstr, const char *fmt,...)
