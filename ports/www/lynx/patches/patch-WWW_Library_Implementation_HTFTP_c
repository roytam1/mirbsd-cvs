$MirOS$

	Fix spelling; suggested for upstream inclusion.

--- WWW/Library/Implementation/HTFTP.c.orig	Mon Feb 13 02:45:53 2012
+++ WWW/Library/Implementation/HTFTP.c	Sat Mar  3 18:20:20 2012
@@ -1869,7 +1869,7 @@ static void parse_vms_dir_entry(char *li
 	LYLowerCase(entry_info->filename + i);
     }
 
-    /* Uppercase terminal .z's or _z's. */
+    /* Uppercase terminal .zs or _zs. */
     if ((--i > 2) &&
 	entry_info->filename[i] == 'z' &&
 	(entry_info->filename[i - 1] == '.' ||
