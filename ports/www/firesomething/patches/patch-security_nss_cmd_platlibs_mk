$MirOS: ports/www/firesomething/patches/patch-security_nss_cmd_platlibs_mk,v 1.5 2008/05/21 12:23:24 tg Exp $
--- security/nss/cmd/platlibs.mk.orig	2008-02-25 22:32:33.000000000 +0100
+++ security/nss/cmd/platlibs.mk	2008-08-24 14:52:52.000000000 +0200
@@ -243,7 +243,7 @@ endif
 # If GNU ld is used, we must use the -rpath-link option to tell
 # the linker where to find libsoftokn3.so, an implicit dependency
 # of libnss3.so.
-ifeq (,$(filter-out BSD_OS FreeBSD Linux NetBSD, $(OS_ARCH)))
+ifeq (,$(filter-out BSD_OS FreeBSD Linux MidnightBSD MirBSD NetBSD OpenBSD, $(OS_ARCH)))
 EXTRA_SHARED_LIBS += -Wl,-rpath-link,$(DIST)/lib
 endif
 
