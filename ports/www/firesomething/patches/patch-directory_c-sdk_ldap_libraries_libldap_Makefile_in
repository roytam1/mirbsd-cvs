$MirOS: ports/www/firesomething/patches/patch-directory_c-sdk_ldap_libraries_libldap_Makefile_in,v 1.2 2006/05/11 20:55:43 bsiegert Exp $
--- directory/c-sdk/ldap/libraries/libldap/Makefile.in.orig	Thu Dec  8 21:10:47 2005
+++ directory/c-sdk/ldap/libraries/libldap/Makefile.in	Thu Dec  8 21:11:07 2005
@@ -229,6 +229,14 @@ ifeq ($(OS_ARCH), NetBSD)
 EXTRA_LIBS = -L$(dist_libdir) -l$(LBER_LIBNAME)
 endif
 
+ifeq ($(OS_ARCH), MidnightBSD)
+EXTRA_LIBS = -L$(dist_libdir) -l$(LBER_LIBNAME) -pthread
+endif
+
+ifeq ($(OS_ARCH), MirBSD)
+EXTRA_LIBS = -L$(dist_libdir) -l$(LBER_LIBNAME) -pthread
+endif
+
 ifeq ($(OS_ARCH), OpenBSD)
 EXTRA_LIBS = -L$(dist_libdir) -l$(LBER_LIBNAME) -pthread
 endif
@@ -286,7 +290,7 @@ $(LIBLDAP): $(OBJS) $(LIBDIR) $(LDAP_EXP
 ifdef SO_FILES_TO_REMOVE
 	-$(RM) $(SO_FILES_TO_REMOVE)
 endif
-ifneq (,$(filter AIX Linux HP-UX Darwin BeOS QNX NetBSD OSF1 OpenBSD, $(OS_ARCH)))
+ifneq (,$(filter AIX BeOS Darwin HP-UX Linux MidnightBSD MirBSD NetBSD OSF1 OpenBSD QNX, $(OS_ARCH)))
 	$(LINK_LIB) ../liblber/$(OBJDIR_NAME)/*.a
 else
 ifeq ($(OS_ARCH),OS2)
