$MirOS$
--- layer.c.orig	Mon Sep  8 14:25:24 2003
+++ layer.c	Sat Jan  9 18:00:23 2010
@@ -458,10 +458,7 @@ int x, y;
 }
 
 void
-LClearLine(l, y, xs, xe, bce, ol)
-struct layer *l;
-int xs, xe, bce;
-struct mline *ol;
+LClearLine(struct layer *l, int y, int xs, int xe, int bce, struct mline *ol)
 {
   struct canvas *cv;
   struct viewport *vp;
