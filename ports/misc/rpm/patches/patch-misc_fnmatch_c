$MirOS$
--- misc/fnmatch.c.orig	Wed Jun 14 12:34:09 2000
+++ misc/fnmatch.c	Fri Jul 11 12:48:14 2008
@@ -18,6 +18,8 @@
 
 # include "system.h"
 
+static inline char *__strchrnul(const char *s, int c);
+
 /* Find the first occurrence of C in S or the final NUL byte.  */
 static inline char *
 __strchrnul (s, c)
@@ -237,7 +239,7 @@ __strchrnul (s, c)
    whose names are inconsistent.  */
 
 # if !defined _LIBC && !defined getenv
-extern char *getenv ();
+extern char *getenv (const char *);
 # endif
 
 # ifndef errno
