$MirOS$
--- Makefile.PL.orig	Mon Mar 17 20:25:47 2008
+++ Makefile.PL	Mon Mar 17 20:26:06 2008
@@ -23,10 +23,6 @@ if (eval { require YAML::Syck; $YAML::Sy
     $bad++;
 }
 
-if ($bad and !is_admin()) {
-    exit() unless prompt("Continue installing YAML::Syck?", 'y') =~ /^y/i;
-}
-
 name            'YAML-Syck';
 license         'MIT';
 all_from        'lib/YAML/Syck.pm';
