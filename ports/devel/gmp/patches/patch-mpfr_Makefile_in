$MirOS$
--- mpfr/Makefile.in.orig	Wed Sep 22 20:26:02 2004
+++ mpfr/Makefile.in	Wed Jan 25 13:43:12 2006
@@ -270,6 +270,7 @@ install-libLIBRARIES: $(lib_LIBRARIES)
 	@list='$(lib_LIBRARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
 	    echo " $(RANLIB) $(DESTDIR)$(libdir)/$$p"; \
+	    chmod u+w $(DESTDIR)$(libdir)/$$p; \
 	    $(RANLIB) $(DESTDIR)$(libdir)/$$p; \
 	  else :; fi; \
 	done
