$MirOS$
--- libs.am.orig	Wed May  6 07:18:20 1998
+++ libs.am	Mon Mar  6 23:08:21 2006
@@ -36,6 +36,7 @@ install-@DIR@LIBRARIES: $(@DIR@_LIBRARIE
 	  if test -f $$p; then \
 ## Must ranlib after installing because mod time changes.
 	    echo " $(RANLIB) $(DESTDIR)$(@DIR@dir)/$$p"; \
+	    chmod u+w $(DESTDIR)$(@DIR@dir)/$$p; \
 	    $(RANLIB) $(DESTDIR)$(@DIR@dir)/$$p; \
 	  else :; fi; \
 	done
