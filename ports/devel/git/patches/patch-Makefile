$MirOS$
--- Makefile.orig	Wed May 13 06:50:28 2009
+++ Makefile	Sun May 31 16:19:20 2009
@@ -189,8 +189,8 @@ uname_V := $(shell sh -c 'uname -v 2>/de
 
 # CFLAGS and LDFLAGS are for the users to override from the command line.
 
-CFLAGS = -g -O2 -Wall
-LDFLAGS =
+CFLAGS ?= -g -O2 -Wall
+LDFLAGS ?=
 ALL_CFLAGS = $(CFLAGS)
 ALL_LDFLAGS = $(LDFLAGS)
 STRIP ?= strip
@@ -207,28 +207,22 @@ STRIP ?= strip
 # runtime figures out where they are based on the path to the executable.
 # This can help installing the suite in a relocatable way.
 
-prefix = $(HOME)
+#prefix = $(HOME)
 bindir_relative = bin
 bindir = $(prefix)/$(bindir_relative)
-mandir = share/man
+mandir = man
 infodir = share/info
-gitexecdir = libexec/git-core
+gitexecdir = libexec/git
 sharedir = $(prefix)/share
 template_dir = share/git-core/templates
 htmldir = share/doc/git-doc
-ifeq ($(prefix),/usr)
-sysconfdir = /etc
-ETC_GITCONFIG = $(sysconfdir)/gitconfig
-else
-sysconfdir = $(prefix)/etc
-ETC_GITCONFIG = etc/gitconfig
-endif
+ETC_GITCONFIG = $(SYSCONFDIR)/gitconfig
 lib = lib
 # DESTDIR=
 
 # default configuration for gitweb
 GITWEB_CONFIG = gitweb_config.perl
-GITWEB_CONFIG_SYSTEM = /etc/gitweb.conf
+GITWEB_CONFIG_SYSTEM = $(SYSCONFDIR)/gitweb.conf
 GITWEB_HOME_LINK_STR = projects
 GITWEB_SITENAME =
 GITWEB_PROJECTROOT = /pub/git
@@ -246,16 +240,16 @@ GITWEB_SITE_FOOTER =
 
 export prefix bindir sharedir sysconfdir
 
-CC = gcc
+CC ?= gcc
 AR = ar
 RM = rm -f
 TAR = tar
 FIND = find
 INSTALL = install
 RPMBUILD = rpmbuild
-TCL_PATH = tclsh
-TCLTK_PATH = wish
-PTHREAD_LIBS = -lpthread
+TCL_PATH ?= tclsh
+TCLTK_PATH ?= wish
+PTHREAD_LIBS = -pthread
 
 export TCL_PATH TCLTK_PATH
 
@@ -689,6 +683,8 @@ ifeq ($(uname_S),Darwin)
 	NO_MEMMEM = YesPlease
 	THREADED_DELTA_SEARCH = YesPlease
 	USE_ST_TIMESPEC = YesPlease
+	NO_FINK = YesPlease
+	NO_DARWIN_PORTS = YesPlease
 endif
 ifeq ($(uname_S),SunOS)
 	NEEDS_SOCKET = YesPlease
@@ -747,13 +743,13 @@ ifeq ($(uname_S),FreeBSD)
 	endif
 endif
 ifeq ($(uname_S),OpenBSD)
-	NO_STRCASESTR = YesPlease
-	NO_MEMMEM = YesPlease
-	NEEDS_LIBICONV = YesPlease
-	BASIC_CFLAGS += -I/usr/local/include
-	BASIC_LDFLAGS += -L/usr/local/lib
-	THREADED_DELTA_SEARCH = YesPlease
+	BASIC_CFLAGS += ${mp_BASIC_CFLAGS}
+	BASIC_LDFLAGS += ${mp_BASIC_LDFLAGS}
 endif
+ifeq ($(uname_S),MirBSD)
+	BASIC_CFLAGS += ${mp_BASIC_CFLAGS}
+	BASIC_LDFLAGS += ${mp_BASIC_LDFLAGS}
+endif
 ifeq ($(uname_S),NetBSD)
 	ifeq ($(shell expr "$(uname_R)" : '[01]\.'),2)
 		NEEDS_LIBICONV = YesPlease
@@ -918,7 +914,7 @@ ifndef NO_POSIX_ONLY_PROGRAMS
 	PROGRAMS += git-imap-send$X
 endif
 ifndef NO_OPENSSL
-	OPENSSL_LIBSSL = -lssl
+	OPENSSL_LIBSSL = -lssl -lcrypto
 	ifdef OPENSSLDIR
 		BASIC_CFLAGS += -I$(OPENSSLDIR)/include
 		OPENSSL_LINK = -L$(OPENSSLDIR)/$(lib) $(CC_LD_DYNPATH)$(OPENSSLDIR)/$(lib)
@@ -1479,7 +1475,7 @@ all:: $(TEST_PROGRAMS)
 
 export NO_SVN_TESTS
 
-test: all
+test:
 	$(MAKE) -C t/ all
 
 test-ctype$X: ctype.o
