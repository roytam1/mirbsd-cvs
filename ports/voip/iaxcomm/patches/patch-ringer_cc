$MirOS$

XXX /dev/speaker or something like that, anyone?

--- ringer.cc.orig	Fri Jun 17 23:27:34 2005
+++ ringer.cc	Fri Jun 17 23:31:23 2005
@@ -58,6 +58,7 @@
 #ifdef __WXGTK__
   #include <sys/ioctl.h>
   #include <sys/fcntl.h>
+#ifdef __linux__
   #include <linux/kd.h>
 
 static void Beep(int freq, int dur)
@@ -69,6 +70,13 @@ static void Beep(int freq, int dur)
    arg = (dur<<16)+(1193180/freq);
    ioctl(fd,KDMKTONE,arg);
 }
+#else
+static void Beep(int freq, int dur)
+{
+	fputc('\a', stderr);
+	fflush(stderr);
+}
+#endif
 #endif
 
 //----------------------------------------------------------------------------------------
