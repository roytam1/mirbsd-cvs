$MirOS$
--- unixfuncs.c.orig	Thu Feb  3 21:19:16 2005
+++ unixfuncs.c	Sat Jun 18 00:09:56 2005
@@ -376,3 +376,19 @@ int iaxc_prioboostend()  {
 
 #endif
 
+#ifdef __OpenBSD__
+#define PTHREAD_MIN_PRIORITY                    0
+#define PTHREAD_MAX_PRIORITY                    31      /* 0x1F */
+
+int
+sched_get_priority_max(int x)
+{
+	return (PTHREAD_MAX_PRIORITY);
+}
+
+int
+sched_get_priority_min(int x)
+{
+	return (PTHREAD_MIN_PRIORITY);
+}
+#endif
