$MirOS$
--- src/om/etc/genshlib.orig	Mon Nov  8 15:04:22 2004
+++ src/om/etc/genshlib	Fri Nov 24 03:07:28 2006
@@ -38,6 +38,8 @@ do
   shift
 done
 
+vers=$(echo "$vers" | { IFS=. read a b c; echo "$a.${b-0}"; })
+
 destsystem="$1"
 
 if [ -z "$destsystem" ] || [ -z "$srcsystems" ]
