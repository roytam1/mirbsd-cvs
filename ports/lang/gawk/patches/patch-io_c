$MirOS$
--- io.c.orig	Fri Jul  4 17:50:36 2003
+++ io.c	Mon Dec 15 21:14:07 2008
@@ -64,6 +64,8 @@
 #include <process.h>
 #endif
 
+#include <sys/ioctl.h>
+
 #ifndef ENFILE
 #define ENFILE EMFILE
 #endif
@@ -567,7 +569,7 @@ redirect(NODE *tree, int *errflg)
 			if (do_lint && rpflag != newflag)
 				lintwarn(
 		_("unnecessary mixing of `>' and `>>' for file `%.*s'"),
-					tmp->stlen, rp->value);
+					(int)tmp->stlen, rp->value);
 
 			break;
 		}
@@ -819,7 +821,7 @@ do_close(NODE *tree)
 
 		if (do_lint)
 			lintwarn(_("close: `%.*s' is not an open file, pipe or co-process"),
-				tmp->stlen, tmp->stptr);
+				(int)tmp->stlen, tmp->stptr);
 
 		/* update ERRNO manually, using errno = ENOENT is a stretch. */
 		cp = _("close of redirection that was never opened");
