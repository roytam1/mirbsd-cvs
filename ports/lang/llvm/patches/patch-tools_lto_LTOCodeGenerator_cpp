$MirOS$

	1) Support branding, especially via CPPFLAGS
	2) Fix whitespace mistake

--- tools/lto/LTOCodeGenerator.cpp.orig	Thu Jan  1 00:00:00 1970
+++ tools/lto/LTOCodeGenerator.cpp	Mon Nov 10 00:59:12 2008
@@ -56,11 +56,14 @@ using namespace llvm;
 static cl::opt<bool> DisableInline("disable-inlining",
   cl::desc("Do not run the inliner pass"));
 
+#ifdef LLVM_VERSION_INFO_SVN
+#define LLVM_VERSION_INFO " SVN " LLVM_VERSION_INFO_SVN
+#endif
 
 const char* LTOCodeGenerator::getVersionString()
 {
 #ifdef LLVM_VERSION_INFO
-    return PACKAGE_NAME " version " PACKAGE_VERSION ", " LLVM_VERSION_INFO;
+    return PACKAGE_NAME " version " PACKAGE_VERSION "," LLVM_VERSION_INFO;
 #else
     return PACKAGE_NAME " version " PACKAGE_VERSION;
 #endif
