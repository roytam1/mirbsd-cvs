$MirOS$
--- utils/unittest/googletest/include/gtest/internal/gtest-port.h.orig	Thu Jan  1 01:00:00 1970
+++ utils/unittest/googletest/include/gtest/internal/gtest-port.h	Fri Dec  4 21:31:18 2009
@@ -227,9 +227,10 @@
 // TODO(wan@google.com): uses autoconf to detect whether ::std::wstring
 //   is available.
 
-#if defined(GTEST_OS_CYGWIN) || defined(GTEST_OS_SOLARIS) || defined(GTEST_OS_HAIKU)
+#if defined(GTEST_OS_CYGWIN) || defined(GTEST_OS_SOLARIS) || defined(GTEST_OS_HAIKU) || defined(__OpenBSD__)
 // At least some versions of cygwin don't support ::std::wstring.
 // Solaris' libc++ doesn't support it either.
+// OpenBSD doesn't have it either.
 #define GTEST_HAS_STD_WSTRING 0
 #else
 #define GTEST_HAS_STD_WSTRING GTEST_HAS_STD_STRING
