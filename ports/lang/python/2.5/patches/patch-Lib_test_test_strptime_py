$MirOS$
--- Lib/test/test_strptime.py.orig	Mon Jun  4 00:13:43 2007
+++ Lib/test/test_strptime.py	Wed May 28 20:56:00 2014
@@ -515,6 +515,9 @@ class CacheTests(unittest.TestCase):
                          id(_strptime._TimeRE_cache.locale_time))
 
     def test_TimeRE_recreation(self):
+        if sys.platform.startswith('mirbsd'):
+            # only one locale here
+            return
         # The TimeRE instance should be recreated upon changing the locale.
         locale_info = locale.getlocale(locale.LC_TIME)
         try:
