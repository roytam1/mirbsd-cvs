$MirOS$
--- source/amx/amxtime.c.orig	Fri Jan 25 16:46:01 2008
+++ source/amx/amxtime.c	Fri Aug  8 19:31:10 2008
@@ -28,6 +28,13 @@
   #include <mmsystem.h>
 #endif
 
+#ifdef BSD
+#define stime(t) do {				\
+	struct timeval tv = { *(t), 0 };	\
+	settimeofday(&tv, NULL);		\
+} while (0)
+#endif
+
 #define CELLMIN   (-1 << (8*sizeof(cell) - 1))
 
 #define SECONDS_PER_MINUTE	60
