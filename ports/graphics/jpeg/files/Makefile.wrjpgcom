# $MirOS$

SRCDIR=		${.CURDIR}/..
BINDIR=		${PREFIX}/bin
MANDIR=		${PREFIX}/man/cat

PROG=		wrjpgcom
SRCS=		wrjpgcom.c
MAN=		wrjpgcom.1
LDADD+=		-L${SRCDIR}/lib -ljpeg

.PATH:		${SRCDIR}

.if defined(DESTDIR) && !empty(DESTDIR) && defined(TRUEPREFIX)
PREFIX:=	${TRUEPREFIX}
.endif

.include <bsd.prog.mk>
