$MirOS$
--- print-l2tp.c.orig	Thu Jan  1 00:00:00 1970
+++ print-l2tp.c	Mon Jul  9 19:08:40 2007
@@ -690,8 +690,11 @@ l2tp_print(const u_char *dat, u_int leng
 	}
 
 	if (flag_o) {
+		u_char *tmp;
+
 		pad =  ntohs(*ptr++);
-		(u_char *)ptr += pad;
+		tmp = (u_char *)ptr + pad;
+		ptr = (u_short *)tmp;
 		cnt += (2 + pad);
 	}
 
