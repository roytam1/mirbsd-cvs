$MirOS$
--- src/language.c.orig	Mon Jan 12 08:12:43 1998
+++ src/language.c	Thu Mar 22 00:50:55 2007
@@ -469,7 +469,7 @@ static int make_indexfile(char *indexfil
 	return -1;
     }
     if (verbose || total_msgs != nmsg)
-	fprintf(stderr, "%d messages, %d translations\n\n", total_msgs, nmsg);
+	fprintf(stderr, "%d messages, %ld translations\n\n", total_msgs, nmsg);
 
     if ((indexf = fopen(indexfile, FOPWBIN)) == NULL) {
 	fprintf(stderr, "Cannot create %s\n", indexfile);
