$MirOS$
--- bmc-watchdog/src/bmc-watchdog.c.orig	Fri Dec 15 17:26:21 2006
+++ bmc-watchdog/src/bmc-watchdog.c	Wed Nov  6 19:45:34 2013
@@ -237,7 +237,7 @@ _bmclog(const char *fmt, ...)
   if ((tm = localtime(&t)) == NULL)
     {
       /* Just use the value from time() */
-      snprintf(buffer, BMC_WATCHDOG_ERR_BUFLEN, "%ld: %s\n", t, fmt);
+      snprintf(buffer, BMC_WATCHDOG_ERR_BUFLEN, "%lld: %s\n", (long long)t, fmt);
     }
   else
     {
