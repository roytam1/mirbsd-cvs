$MirOS$
--- Src/Builtins/sched.c.orig	Mon Aug  4 15:16:50 2008
+++ Src/Builtins/sched.c	Sun Sep  6 15:32:56 2009
@@ -352,7 +352,7 @@ schedgetfn(UNUSED(Param pm))
 	time_t t;
 
 	t = sch->time;
-	sprintf(tbuf, "%ld", t);
+	sprintf(tbuf, "%lld", (long long)t);
 	if (sch->flags & SCHEDFLAG_TRASH_ZLE)
 	    flagstr = "-o";
 	else
