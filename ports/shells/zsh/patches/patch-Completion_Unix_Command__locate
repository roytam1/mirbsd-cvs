$MirOS$
--- Completion/Unix/Command/_locate.orig	Tue May 27 10:24:29 2008
+++ Completion/Unix/Command/_locate	Sun Sep  6 15:16:29 2009
@@ -26,7 +26,7 @@ case $basename in
     ;;
 
     (*illegal option*)
-    if [[ $OSTYPE == (freebsd|openbsd|dragonfly|darwin)* ]]; then
+    if [[ $OSTYPE == (freebsd|mirbsd|openbsd|dragonfly|darwin)* ]]; then
       ltype=bsd
     else
       ltype=$best_guess
