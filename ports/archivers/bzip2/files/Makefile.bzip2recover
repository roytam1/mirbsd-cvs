# $MirOS$

SRCDIR=		${.CURDIR}/..
BINDIR=		${PREFIX}/bin
MANDIR=		${PREFIX}/man/cat

PROG=		bzip2recover
SRCS=		bzip2recover.c
LDADD+=		-L${SRCDIR}/lib -lbz2

NOMAN=		Yes

.PATH:		${SRCDIR}

.if defined(DESTDIR) && !empty(DESTDIR) && defined(TRUEPREFIX)
PREFIX:=	${TRUEPREFIX}
.endif

.include <bsd.prog.mk>
