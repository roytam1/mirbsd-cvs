$MirOS$
--- src/rrd_tool.h.orig	Mon Apr 25 21:03:25 2005
+++ src/rrd_tool.h	Wed Nov 12 20:06:23 2008
@@ -40,6 +40,7 @@ extern "C" {
 #include <string.h>
 #include <time.h>
 #include <ctype.h>
+#include <getopt.h>
 
 #if HAVE_SYS_PARAM_H
 #  include <sys/param.h>
@@ -90,7 +91,6 @@ int isnan(double value);
 #endif
 
 /* local include files -- need to be after the system ones */
-#include "getopt.h"
 #include "rrd_format.h"
 
 #ifndef max
@@ -126,7 +126,6 @@ info_t *rrd_info(int, char **);
 
 /* HELPER FUNCTIONS */
 int GifSize(FILE *, long *, long *);
-int PngSize(FILE *, long *, long *);
 int PngSize(FILE *, long *, long *);
 
 #include <gd.h>
