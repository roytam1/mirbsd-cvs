$MirOS$
--- src/screen.c.orig	Sun Jan 13 10:36:04 2008
+++ src/screen.c	Wed Feb 27 23:11:17 2008
@@ -1464,7 +1464,11 @@ void scr_DrawMainWindow(unsigned int ful
   if (fullinit) {
     // Enable keypad (+ special keys)
     keypad(inputWnd, TRUE);
+#ifdef __MirBSD__
+    wtimeout(inputWnd, 50 /* ms */);
+#else
     nodelay(inputWnd, TRUE);
+#endif
 
     // Create panels
     rosterPanel = new_panel(rosterWnd);
