$MirOS$
--- RawIP.xs.orig	Wed Apr  8 00:20:32 2009
+++ RawIP.xs	Wed Apr  8 00:20:33 2009
@@ -441,6 +441,7 @@ static SV * ip_opts_creat(ref)
                          SvCUR(*av_fetch(opts,i+2,0)));
        break;
        default:
+       break;
        }
     }
        c = 0;
@@ -540,6 +541,7 @@ static SV * tcp_opts_creat(ref)
                          SvCUR(*av_fetch(opts,i+2,0)));
        break;
        default:
+       break;
        }
     }
        c = 0;
