$MirOS$
--- lib/url.c.orig	Tue Apr 10 20:46:17 2007
+++ lib/url.c	Tue May  8 20:36:51 2007
@@ -606,6 +606,10 @@ CURLcode Curl_open(struct SessionHandle 
     /* This is our preferred CA cert bundle since install time */
     data->set.ssl.CAfile = (char *)CURL_CA_BUNDLE;
 #endif
+#ifdef CURL_CA_PATH
+    /* This is our preferred CA cert path at build time */
+    data->set.ssl.CApath = (char *)CURL_CA_PATH;
+#endif
   }
 
   if(res) {
