$MirOS$
--- src/mkhelp.pl.orig	Mon Oct  9 14:58:29 2006
+++ src/mkhelp.pl	Sat Apr  5 19:12:16 2008
@@ -76,7 +76,7 @@ close(READ);
 # if compressed
 if($c) {
     my @test = `gzip --version 2>&1`;
-    if($test[0] =~ /gzip/) {
+    if(($test[0] =~ /gzip/) || ($test[0] =~ /MirOS/) || ($test[0] =~ /OpenBSD/)) {
         open(GZIP, ">dumpit") ||
             die "can't create the dumpit file, try without -c";
         binmode GZIP;
