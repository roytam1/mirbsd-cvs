$MirOS$
--- hw/pc.c.orig	Sun Jan  6 19:38:19 2008
+++ hw/pc.c	Wed May  7 14:29:24 2008
@@ -435,6 +435,7 @@ static void generate_bootsect(uint32_t g
     bdrv_set_boot_sector(drives_table[hda].bdrv, bootsect, sizeof(bootsect));
 }
 
+#if 0
 static int load_kernel(const char *filename, uint8_t *addr,
                        uint8_t *real_addr)
 {
@@ -465,6 +466,7 @@ static int load_kernel(const char *filen
     close(fd);
     return -1;
 }
+#endif
 
 static long get_file_size(FILE *f)
 {
