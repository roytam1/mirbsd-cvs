#!/bin/sh
# $MirOS$
#
# redhat_base de-installation

PATH=/bin:/usr/bin:/sbin:/usr/sbin
PREFIX=${PKG_PREFIX:-/usr/local}

$PREFIX/emul/linux/linuxctl deregister ${PREFIX}/emul/redhat \
    bin etc lib sbin usr var

exit 0
