$MirOS$
--- src/interfaces/ecpg/pgtypeslib/interval.c.orig	Wed Oct  4 02:29:48 2006
+++ src/interfaces/ecpg/pgtypeslib/interval.c	Thu Feb 22 22:06:09 2007
@@ -482,7 +482,7 @@ EncodeInterval(struct tm * tm, fsec_t fs
 			if (tm->tm_year != 0)
 			{
 				sprintf(cp, "%d year%s",
-						tm->tm_year, (tm->tm_year != 1) ? "s" : "");
+						(int)tm->tm_year, (tm->tm_year != 1) ? "s" : "");
 				cp += strlen(cp);
 				is_before = (tm->tm_year < 0);
 				is_nonzero = TRUE;
