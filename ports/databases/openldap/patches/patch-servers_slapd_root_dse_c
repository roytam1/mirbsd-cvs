$MirOS$
--- servers/slapd/root_dse.c.orig	Thu Jan 20 17:00:47 2005
+++ servers/slapd/root_dse.c	Wed May 20 14:39:58 2009
@@ -173,7 +173,7 @@ root_dse_info(
 
 	/* supportedLDAPVersion */
 	for ( i=LDAP_VERSION_MIN; i<=LDAP_VERSION_MAX; i++ ) {
-		char buf[BUFSIZ];
+		char buf[16];
 		if (!( global_allows & SLAP_ALLOW_BIND_V2 ) &&
 			( i < LDAP_VERSION3 ) )
 		{
