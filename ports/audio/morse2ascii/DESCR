experimental tool for decoding the morse codes from a PCM WAV file using
a volume/peak based method. the tool can also decode the morse codes
from text and RAW PCM files. it contains some options for parsing
abbreviations, prosigns and qcodes.
