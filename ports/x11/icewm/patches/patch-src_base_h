$MirOS$
--- src/base.h.orig	Sun Sep 10 16:11:48 2006
+++ src/base.h	Thu Oct 18 17:26:26 2012
@@ -60,6 +60,8 @@ inline T clamp(T value, T minimum, T max
     return max(min(value, maximum), minimum);
 }
 
+#undef abs
+
 template <class T>
 inline T abs(T v) {
     return (v < 0 ? -v : v);
