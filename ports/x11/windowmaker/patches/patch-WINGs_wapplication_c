$MirOS$
--- WINGs/wapplication.c.orig	2005-02-02 19:21:24.000000000 +0100
+++ WINGs/wapplication.c	2005-02-02 19:22:19.000000000 +0100
@@ -175,7 +175,7 @@ WMPathForResourceOfType(char *resource, 
         }
     }
 
-    path = checkFile("/usr/local/GNUstep", appdir, ext, resource);
+    path = checkFile("%%%PREFIX%%%/GNUstep", appdir, ext, resource);
     if (path) {
         wfree(appdir);
         return path;
